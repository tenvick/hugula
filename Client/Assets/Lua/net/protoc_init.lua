-----------------this is generate by hugula lua protobuf tool -------------------------
-------------------------do not change--------------------------------------
local pb = require("pb")
local protoc = require "protoc"
local p = protoc.new()
p:loadfile("comm.proto")
p:loadfile("commcode.proto")
p:loadfile("login.proto")
p:loadfile("_define.proto")
